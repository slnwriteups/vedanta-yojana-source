diff --git a/node_modules/metro/node_modules/image-size/dist/types/icns.js b/node_modules/metro/node_modules/image-size/dist/types/icns.js
index f2bfafe..2f2e64e 100644
--- a/node_modules/metro/node_modules/image-size/dist/types/icns.js
+++ b/node_modules/metro/node_modules/image-size/dist/types/icns.js
@@ -82,7 +82,7 @@ exports.ICNS = {
         let imageOffset = SIZE_HEADER;
         let imageHeader = readImageHeader(input, imageOffset);
         let imageSize = getImageSize(imageHeader[0]);
-        imageOffset += imageHeader[1];
+        imageOffset += imageHeader[1] > 0 ? imageHeader[1] : SIZE_HEADER; // guard: a crafted zero-length entry must not stall the loop
         if (imageOffset === fileLength)
             return imageSize;
         const result = {
@@ -93,7 +93,7 @@ exports.ICNS = {
         while (imageOffset < fileLength && imageOffset < inputLength) {
             imageHeader = readImageHeader(input, imageOffset);
             imageSize = getImageSize(imageHeader[0]);
-            imageOffset += imageHeader[1];
+            imageOffset += imageHeader[1] > 0 ? imageHeader[1] : SIZE_HEADER; // guard: a crafted zero-length entry must not stall the loop
             result.images.push(imageSize);
         }
         return result;
diff --git a/node_modules/metro/node_modules/image-size/dist/types/jxl.js b/node_modules/metro/node_modules/image-size/dist/types/jxl.js
index f557b0a..7ad17ac 100644
--- a/node_modules/metro/node_modules/image-size/dist/types/jxl.js
+++ b/node_modules/metro/node_modules/image-size/dist/types/jxl.js
@@ -24,7 +24,7 @@ function extractPartialStreams(input) {
         if (!jxlpBox)
             break;
         partialStreams.push(input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size));
-        offset = jxlpBox.offset + jxlpBox.size;
+        offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8); // guard: a crafted zero-size box must not stall the loop
     }
     return partialStreams;
 }
